"use strict";
// tokens delete command
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const commandline_1 = require("../../util/commandline");
const interaction_1 = require("../../util/interaction");
const apis_1 = require("../../util/apis");
let AppDeleteCommand = class AppDeleteCommand extends commandline_1.Command {
    constructor(args) {
        super(args);
    }
    run(client) {
        return __awaiter(this, void 0, void 0, function* () {
            const confirmation = yield interaction_1.prompt.confirm(`Do you really want to delete the token with ID "${this.id}"`);
            if (confirmation) {
                const deleteTokenResponse = yield interaction_1.out.progress("Deleting token ...", apis_1.clientRequest((cb) => client.apiTokens.deleteMethod(this.id, cb)));
                if (deleteTokenResponse.response.statusCode === 404) {
                    return commandline_1.failure(commandline_1.ErrorCodes.InvalidParameter, `the token with ID "${this.id}" could not be found`);
                }
            }
            else {
                interaction_1.out.text(`Deletion of token with ID "${this.id}" canceled`);
            }
            return commandline_1.success();
        });
    }
};
__decorate([
    commandline_1.help("ID of the API token"),
    commandline_1.name("token"),
    commandline_1.required,
    commandline_1.position(0)
], AppDeleteCommand.prototype, "id", void 0);
AppDeleteCommand = __decorate([
    commandline_1.help("Delete an API token")
], AppDeleteCommand);
exports.default = AppDeleteCommand;
