import FileUploadController from "../controller";
export default class SingleThreadUploadController extends FileUploadController {
    private runningUploadRequests;
    healthCheck(): void;
    cancel(): void;
    upload(): void;
    private uploadChunk;
}
