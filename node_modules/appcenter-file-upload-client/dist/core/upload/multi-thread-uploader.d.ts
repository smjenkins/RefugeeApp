import FileUploadController from "../controller";
import { IUploadData } from "../../types";
export default class MultiThreadUploadController extends FileUploadController {
    constructor(uploadData: IUploadData);
    healthCheck(): void;
    stopAllWorkers(): void;
    private removeInflightChunk;
    private removeWorker;
    private initWorkers;
    private setupWorker;
    private startWorker;
    private dispatchWorker;
}
